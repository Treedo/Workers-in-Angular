{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve treedo & Launch Chrome",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "startTreedo",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
